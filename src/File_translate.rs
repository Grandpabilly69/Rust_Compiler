use std::fs;
use std::io;

// pub fn get_file_contents<'a>(file_path: &str, buffer: &'a mut String) -> io::Result<&'a str> {
//     *buffer = fs::read_to_string(file_path)?;
//     Ok(buffer.as_str())
// }
// pub fn read_file(file_path: &str) -> io::Result<()> {
//     let contents = get_file_contents(file_path)?;
//     println!("File content:\n{}", contents);
//     Ok(())
// }

